name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'
        
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: 8.2
        
    - name: Make gradlew executable
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew assembleDebug --no-daemon --stacktrace
      
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: aidukan-app
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
        
    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%d-%H%M')" >> $GITHUB_OUTPUT
        
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0-${{ steps.date.outputs.date }}
        name: Aidukan v1.0 - ${{ steps.date.outputs.date }}
        body: |
          🎉 **Aidukan Android App - Ready to Install!**
          
          ## 📱 Quick Install:
          1. Download `app-debug.apk` below
          2. Transfer to Android phone
          3. Open and tap Install
          4. Grant permissions (Microphone, Camera, Notifications)
          5. Start using! 🚀
          
          ## ✨ Features Included:
          - 🎤 **Voice Assistant** - Say "GWTS" or tap to speak
          - 📸 **AI Photo Recognition** - Take product photos
          - ⏰ **Auto Scheduling** - Daily reports, alerts
          - ☁️ **Cloud Sync** - Backup your data
          - 🔔 **Smart Notifications** - Stay updated
          - 💬 **WhatsApp Integration** - Send bills
          - 📊 **Market Insights** - AI recommendations
          
          ## 🔑 Optional Setup (For Full AI):
          - **Gemini AI**: Get free key at https://makersuite.google.com/app/apikey
          - **Supabase**: Create free account at https://supabase.com
          
          See `SETUP_GUIDE.md` for detailed instructions.
          
          ## 📋 Requirements:
          - Android 7.0 or higher
          - 50 MB free space
          - Microphone & Camera
          
          ---
          
          **Built with ❤️ using Bhindi PRO**
          
          *Your AI-powered kirana management system!*
        files: app/build/outputs/apk/debug/app-debug.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
